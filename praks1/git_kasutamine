
* root kasutajana:
installi - apt-get install git
* tavakasutajana:
esmakordselt:

a) seadista - määra kasutajanimi, email, editor
git config --global user.name "Kevin Inginen-Tosuk"
git config --global user.email kevin.inginen-tosuk@voco.ee
git config --global core.editor nano

b) loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git süsteemi jaoks antud kaustas
git init

remote seadistamine:
a1) git remote add nimi github:aadress
git remote add skriptimine git@github.com:loqie-666/reponimi.git

b1)git remote kontroll
git remote -v
